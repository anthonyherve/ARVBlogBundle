{% extends 'ARVBlogBundle::blog.html.twig' %}

{% block content_page %}

    <h1>{{ 'arv.blog.title.h1.edit_comment'|trans }}</h1>

    {% include 'ARVBlogBundle:Comment:form.html.twig' with {'form': edit_form} %}

    <ul>
        <li>
            <a href="{{ path('arv_blog_article_show', {'id': comment.article.id, 'slug': comment.article.slug}) }}">
                {{ 'arv.blog.action.back_article'|trans }}
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>

{% endblock %}
