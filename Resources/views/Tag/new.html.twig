{% extends 'ARVBlogBundle::blog.html.twig' %}

{% block body %}
    {{ parent() }}

    <h1>Ajouter un tag</h1>

    {% include 'ARVBlogBundle:Comment:form.html.twig' with {'form': form} %}

    <ul class="record_actions">
        <li>
            <a href="{{ path('arv_blog_tag') }}">
                Retour à la liste
            </a>
        </li>
    </ul>
{% endblock %}
