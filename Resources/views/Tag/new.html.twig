{% extends 'ARVBlogBundle::blog.html.twig' %}

{% block content_page %}

    <h1>{{ 'arv.blog.title.h1.add_tag'|trans }}</h1>

    {% include 'ARVBlogBundle:Comment:form.html.twig' with {'form': form} %}

    <ul class="record_actions">
        <li>
            <a href="{{ path('arv_blog_tag_manage') }}">
                {{ 'arv.blog.action.back_list'|trans }}
            </a>
        </li>
    </ul>

{% endblock %}
