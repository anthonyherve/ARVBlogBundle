{% extends 'ARVBlogBundle::blog.html.twig' %}

{% block content %}
    <h1>{{ 'arv.blog.title.h1.add_article'|trans }}</h1>

    {% include 'ARVBlogBundle:Article:form.html.twig' with {'form': form} %}

    <ul>
        <li>
            <a href="{{ path('arv_blog_article_manage') }}">
                {{ 'arv.blog.action.back_list'|trans }}
            </a>
        </li>
    </ul>

{% endblock %}
