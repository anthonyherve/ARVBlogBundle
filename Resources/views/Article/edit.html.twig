{% extends 'ARVBlogBundle::blog.html.twig' %}

{% block content %}

    {{ parent() }}

    <h1>{{ 'arv.blog.title.h1.edit_article'|trans }}</h1>

    {% include 'ARVBlogBundle:Article:form.html.twig' with {'form': edit_form} %}

    <ul>
        <li>
            <a href="{{ path('arv_blog_article_manage') }}">
                {{ 'arv.blog.action.back_list'|trans }}
            </a>
        </li>
        <li>{{ form(delete_form) }}</li>
    </ul>
{% endblock %}
